# Copia este archivo como deploy.env (sin .example) y rellena las credenciales reales.
# deploy.env está en .gitignore: nunca se sube al repositorio.
HOST="ftpupload.net"
USER="if0_41719563"
PASS="tu-contraseña-ftp"
REMOTE_DIR="htdocs/calendario"
